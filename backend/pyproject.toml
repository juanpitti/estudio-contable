[project]
name = "estudio-contable-backend"
version = "0.1.0"
description = "Backend FastAPI — plataforma para estudios contables (AR)"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
